# 공개 메뉴 후속 계획

2026-09-08. 새 기능 출시(A), Google 심사(B), 콘텐츠 개선(C)을 각각 별도로 완료 처리한다.

## A. 메뉴 하나 골라보기 PR과 출시

- [x] 최신 main `b5347ad`에서 별도 브랜치 `codex/public-quick-pick`을 만들고 기능·검사·관련 문서만 분리.
- [x] 기존 독립 QA JSON(두 크기 14개 항목, 오류/차단 0개)과 데스크톱/모바일 결과 캡처 검토. [자료](validation/public-quick-pick-2026-09-08/independent-report.json), [데스크톱](validation/public-quick-pick-2026-09-08/desktop-selected.png), [모바일](validation/public-quick-pick-2026-09-08/mobile-selected.png).
- [x] 분리한 소스에서 전체 단위 테스트·lint·빌드·공개 E2E 재실행.
- [ ] PR 게시 후 리뷰·CI 확인.
- [ ] PR 리뷰와 CI 결과를 확인하고 병합·운영 배포.
- [ ] 실제 운영 홈 → 메뉴 하나 골라보기 → 내부 상세를 클릭. `/recipes`에서도 다시 고르기, 필터 초기화, 단일/빈 후보와 준비 목록 확인. 데스크톱·모바일에서 확인.
- [ ] 배포한 동일 소스를 새로 빌드하고 `npm run verify:public-deployment -- --report /tmp/public-quick-pick-live.json` 실행. 113/113, 사이트맵, 없는 경로 404 모두 통과해야 출시 완료.

배포 전 111/113은 홈·메뉴 본문 차이다. 홈은 로컬 전용 선택 영역을 제거하면 본문이 같다는 후속 점검이 있다. 메뉴 페이지도 같은 공용 컴포넌트를 사용하지만 이 추정으로 배포 후 검사를 생략하지 않는다. Preview가 Production으로 리다이렉트되면 Preview 완료로 간주하지 않는다. 실패 시 최종 도메인·배포 커밋·빌드·캐시·차이 본문을 확인한다.

이번 PR 준비는 병합이나 운영 출시 완료를 의미하지 않는다. 이전 배포 복구·서버 운영 문서의 미커밋 변경은 원래 작업 폴더에 보존한다.

## B. AdSense 심사

기존 로컬 작업 기록에 9/8 14:29 KST 재검토 접수와 ‘준비 중’이 남아 있다. 이번 PR 작업에서 Google 최신 UI를 다시 확인한 것은 아니다.

- [ ] 다음 상태 확인 시 AdSense 사이트 상세에서 도메인·판정·업데이트 시각을 기록한다. Search Console은 검색 색인 진단용으로 따로 확인하며 AdSense 판정의 대체 자료로 쓰지 않는다.
- [ ] 결과가 오면 승인/보완 사유에 맞춰 다음 행동을 정한다. 대기 중 재검토·색인 요청을 반복 제출하지 않는다.

상태 확인 자동화, 광고 활성화, 유료 트래픽 구매는 이 PR에서 설정하지 않는다. 새 기능 배포와 심사 결과를 인과관계로 단정하지 않는다.

## C. 콘텐츠: 다음 별도 PR

1. 홈 후보 선별: 실제 카탈로그에서 추가 핵심 재료와 도구 부담이 적은 메뉴를 조사한다. 레시피 이름만으로 간단하다고 판정하지 않고 원문 재료·분량·단계와 대조한다. 현재 첫 대표 메뉴의 새우·생크림·무염버터 부담을 별도로 해결한다.
2. 달걀·김치·감자·닭고기 허브: 각 허브의 실제 후보를 추가 재료·사용량·도구로 비교하고 선택 이유를 제공한다. 우선 한 허브와 연결 상세를 끝까지 검토한 뒤 확대한다.
3. 미검토 상세의 활용 설명·준비 목록: 원문, 자체 판단, 직접 조리 여부를 분리한다. ‘대체 비율 미확인’ 안내에는 보유한 실제 재료 종류에 맞는 검증된 내부 상세를 연결한다. 시험하지 않은 대체 비율·조리 시간·경험을 만들어 넣지 않는다.
4. 관련 사용자가 탐색 → 상세 → 준비에서 막히는 지점을 피드백으로 확인한다. 분석 지표는 기존 동의 범위에서만 활용하고 승인 목적의 방문자 구매나 광고비를 쓰지 않는다.

완료 기준은 페이지 수가 아니라 후보 선택 이유·준비 재료·원문 조리법의 일치, 로그인 없는 완결된 동선, 검토 출처·범위 표시다. C는 A 출시나 B 승인 전에도 콘텐츠 초안을 준비할 수 있다.

## 날짜 테스트

기존 수정은 각 테스트 전에 Date를 fixture 시각으로 고정하고 종료 후 복원한다. 실제 달력이 흘러 다시 기간을 벗어나는 임시 우회가 아니다. PR에서 이 수정을 독립 커밋으로 보존하며, 2026년과 2030년 시계에 상대적인 ±7일 허용/±8일 거절 및 UTC 날짜 경계를 검사한다. 프로덕션 기간은 변경하지 않는다.

## 이번 PR 검증

Node 24에서 전체 141개 파일·897개 테스트, lint, 빌드와 113개 공개 URL SEO 검사, 공개 E2E 8개(데스크톱 4·모바일 4)가 통과했다. 첫 빌드의 샌드박스 포트 제한 경고는 포트 사용을 허용한 재실행으로 해소했다. `git diff --check`도 통과했다. [실행 요약과 소스 해시](validation/public-quick-pick-2026-09-08/pr-validation.json).

기존 독립 QA는 이전 로컬 실행 증거이며 현재 소스의 재실행 또는 운영 검증과 구분한다. 이번 PR은 운영에 배포하지 않았으므로 운영 113/113과 실제 클릭 검증은 미완료다.
