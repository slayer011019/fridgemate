# Frontend (Vite / Vercel)
# Backend API base URL. Required for deployed frontend.
# Example: https://your-api.up.railway.app/api
VITE_API_URL=
# Optional deployment-controlled override for stale hosting environment values.
VITE_API_URL_OVERRIDE=
# Legacy frontend alias. Leave empty unless older deployment config expects it.
VITE_API_BASE_URL=

# Toggle browser OCR import on or off.
VITE_ENABLE_OCR=true
# Remote correction learning is disabled by default because receipt text may contain personal data.
VITE_ENABLE_REMOTE_IMPORT_LEARNING=false
# Client visibility gate only; the server EXTERNAL_AI_DATA_PROCESSING_ENABLED gate remains authoritative.
VITE_ENABLE_EXTERNAL_AI_DATA_PROCESSING=false

# Optional Sentry DSN for frontend error monitoring.
VITE_SENTRY_DSN=
# Optional Search Console URL-prefix verification. Paste only the meta tag's content value.
# Prefer a DNS TXT record when verifying the whole domain property.
VITE_GOOGLE_SITE_VERIFICATION=
# Optional Naver Search Advisor and Bing Webmaster Tools HTML-tag verification values.
# Paste only each provider's meta tag content value, not the full tag.
VITE_NAVER_SITE_VERIFICATION=
VITE_BING_SITE_VERIFICATION=
# Optional GA4 measurement ID. Analytics loads only after explicit user consent.
VITE_GA_MEASUREMENT_ID=
VITE_ADSENSE_VERIFICATION_ENABLED=false
VITE_ADSENSE_SERVING_ENABLED=false
VITE_ADSENSE_CLIENT=ca-pub-4133464450512249
VITE_ADSENSE_HOME_SLOT=
VITE_ADSENSE_RECIPES_SLOT=

# Required whenever the backend is enabled.
JWT_SECRET=
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=30d
JWT_ISSUER=fridgemate-api
JWT_AUDIENCE=fridgemate-client
PUBLIC_SIGNUP_ENABLED=true
ACCESS_TOKEN_COOKIE_NAME=fridgemate_access
REFRESH_TOKEN_COOKIE_NAME=fridgemate_refresh
# Production HTTPS should use __Host- names, Secure=true, and Path=/.
AUTH_COOKIE_SECURE=false
AUTH_COOKIE_SAME_SITE=Lax
REDIS_URL=redis://localhost:6379
AUTH_REDIS_PREFIX=fridgemate:auth

# Express API
# Local backend port.
PORT=4000
CLIENT_ORIGIN=http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173

# Optional integrations
# Enables AI recipe suggestions when the backend is running.
ANTHROPIC_API_KEY=
# Server/local script only. Never expose the Supabase service role key with a VITE_ prefix.
SUPABASE_URL=
# Public SELECT only. Used by parser dry runs and parser-training exports; never substitute a writer key.
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
FOODSAFETY_API_KEY=
# Enables OCR correction vector suggestions when the backend is running.
OPENAI_API_KEY=
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSIONS=512

# Optional recipe preprocessing and pgvector recommendation support.
# OPENAI_API_KEY above is also used for recipe embedding generation.
# Must match the recipe_embeddings.embedding vector dimension in the migration.
RECIPE_EMBEDDING_MODEL=text-embedding-3-small
RECIPE_EMBEDDING_DIMENSIONS=1536
# Keep false until production vectors and quality gates are verified.
SEMANTIC_RECIPE_API_ENABLED=false
# Global server gate for any user-text transfer to OpenAI or Anthropic.
# Keep false unless the operator has approved external processing and the UI sends a per-request action signal.
EXTERNAL_AI_DATA_PROCESSING_ENABLED=false
# Optional, used only to estimate backfill cost in command summaries.
RECIPE_EMBEDDING_PRICE_PER_MILLION_TOKENS=
# Logs only provider/model/count/token/duration/cost metrics, never prompts or vectors.
AI_USAGE_LOGGING_ENABLED=false
# Recommendation behavior collection is opt-in at both deployment and user-consent levels.
RECOMMENDATION_EVENTS_ENABLED=false
# Product analytics collection is opt-in and authenticated-only.
PRODUCT_EVENTS_ENABLED=false
# Import correction storage and OpenAI embeddings require separate, explicit opt-ins.
IMPORT_CORRECTION_LEARNING_ENABLED=false
IMPORT_CORRECTION_EMBEDDING_ENABLED=false
# Successful requests at or above this duration and all 4xx/5xx responses emit metadata-only telemetry.
API_SLOW_REQUEST_MS=1500

# Database (backend local/dev or production)
# The deployed API connection must use a dedicated login role granted to fridgemate_app.
# Keep the owner/admin credential in DIRECT_URL for trusted migrations only.
# Replace the placeholders below with the connection string from your DB provider.
# Local example:
# DATABASE_URL="postgresql://DB_USER:DB_PASSWORD@localhost:5432/fridgemate?schema=public"
# DIRECT_URL="postgresql://DB_USER:DB_PASSWORD@localhost:5432/fridgemate?schema=public"
# Hosted example:
# DATABASE_URL="postgresql://DB_USER:DB_PASSWORD@DB_HOST:5432/fridgemate?sslmode=require&schema=public"
# DIRECT_URL="postgresql://DB_USER:DB_PASSWORD@DB_HOST:5432/fridgemate?sslmode=require&schema=public"
# Supabase pooler example:
# DATABASE_URL="postgresql://postgres.PROJECT_REF:DB_PASSWORD@aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
# DIRECT_URL="postgresql://postgres.PROJECT_REF:DB_PASSWORD@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres"
DATABASE_URL="postgresql://DB_USER:DB_PASSWORD@DB_HOST:5432/fridgemate?schema=public"
DIRECT_URL="postgresql://DB_USER:DB_PASSWORD@DB_HOST:5432/fridgemate?schema=public"
